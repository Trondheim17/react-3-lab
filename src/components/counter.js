import React from 'react'

const Counter = (props) => {
        return (
            <div className = 'Counter'>
                <div>{props.index}/25</div>
            </div>
        )
    }

export default Counter